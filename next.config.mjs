/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["the-squirrel-pilot.s3.ap-south-1.amazonaws.com","the-squirrel-automation-bot.s3.ap-south-1.amazonaws.com"],
  },
};

export default nextConfig;
