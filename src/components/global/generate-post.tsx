import React from 'react'
import { Button } from '../ui/button'

const GeneratePost = () => {
  return (
    <Button size={"lg"} className='font-semibold'>Generate Post</Button>
  )
}

export default GeneratePost