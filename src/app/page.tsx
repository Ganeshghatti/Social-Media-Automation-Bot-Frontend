import React from "react";
import LoginForm from "@/components/global/login-form";

const page = () => {
  return (
    <section className="h-dvh w-full flex flex-col justify-center items-center bg-[#F3F2F7]">
      <LoginForm />
    </section>
  );
};

export default page;
